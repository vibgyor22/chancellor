{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path') && cd /workspaces/chancellor && git add \"$file\" && git commit -m \"auto: update $(basename $file)\" 2>/dev/null || true",
            "statusMessage": "Committing changes..."
          }
        ]
      }
    ]
  }
}
